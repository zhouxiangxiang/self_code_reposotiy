#include <iostream>
using namespace std;

enum class STATE_MACHINE {
    ST_IDLE,
    ST_STOP, 
};
int main() {
    STATE_MACHINE currentState;

    switch (currentState) {
    case STATE_MACHINE::ST_IDLE:

    case STATE_MACHINE::ST_STOP:

    default:
        break;
    }

	return 0;
}
